import { ShopCategoryPipe } from './shop-category.pipe';

describe('ShopCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
